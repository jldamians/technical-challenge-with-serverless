'use strict';

const repository = {
    /**
     * Buscar vehículo por identificador
     * @param {number} id, identificador
     * @return {object | null}
     */
    async search(id) {
        return null;
    }
};

module.exports = { ...repository };
